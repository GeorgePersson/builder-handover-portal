# Project Worksheet
- 2026-06-26: Pushed project workspace hardening to GitHub on `feature/ai-spec-automation-workflow` at commit `75cedec` (`feat: harden builder project workspace`). Remaining local-only untracked agent folders: `.codex/` and `.hermes/`.
- 2026-06-26: Reworked `docs/phased-work.md` into a cleaner current/future roadmap: completed project-workspace, extraction, review, supplier, quote, client, audit, and production items are now short context only, while each phase lists remaining work. The active next slice is project-workspace hardening with browser smoke tests, visual snapshots, demo smoke, server-action form guardrails, and separate Docling/Turbopack warning cleanup. Verification to run after this docs update: `npm.cmd run lint` and `npm.cmd run build`.
- 2026-06-26: Added the current `/builder/projects` hardening next steps into `docs/phased-work.md`, pointing future agents to `docs/builder-project-workspace-ui-contract.md` as the UI source of truth and listing browser smoke tests, visual snapshots, demo smoke, server-action form warning guardrails, Docling/Turbopack warning follow-up, systemic extraction-fixture discipline, and required lint/build verification. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Fixed regression in `/builder/projects` top project card after screenshot review: Project details and Add client document now sit side by side on wide screens because both are short admin sections, while the server-action document form still avoids explicit `encType`/`method`. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Responded to screenshot feedback on `/builder/projects`: reduced outer page gutters, narrowed the read-only right sidebar so the editable area uses more width, changed Add handover item/item edit forms to stacked vertical entry boxes, renamed the checklist section to `Handover Items & Products`, replaced the old category dropdown/header area with clickable group/category filter buttons showing item counts and ready counts, and kept search/status/source/missing-section filters below as secondary filters. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Made builder dashboard list rows clickable: active projects/client requests/packages link to `/builder/projects?projectId=...`, project workspace now auto-opens the selected project from `projectId`, and upcoming maintenance links to the matching maintenance project anchor. Fixed document upload console warning by removing explicit `encType` from the server-action file form; React/Next supplies multipart handling for function actions.
- 2026-06-26: Updated builder dashboard to remove visible Admin review metric/panel and replace Admin review notifications with a Client requests panel.
- 2026-06-26: On `feature/ai-spec-automation-workflow`, removed the visible builder sidebar `Product Library` nav item while keeping the underlying product/autofill database routes available for later. Database-fill direction: use Codex to build source-backed ingestion/import tooling and GPT/ChatGPT-style models for structured extraction/normalization from supplied PDFs, supplier exports, invoices/manuals, or manufacturer pages — not for inventing unsourced catalogue rows.
- 2026-06-26: Cleaned up GitHub PR clutter.
- 2026-06-26: Pushed local branch handoff to GitHub.
- 2026-06-26: Removed the redundant Search database / Manual add segmented switch from the Add handover item modal on `/builder/projects`.
- 2026-06-26: Cleaned up `/builder/projects` workspace per user correction: moved the handover item list above the Spec sheet automation coming-soon panel, removed the top-header `Project browser` and `Add item` buttons while keeping the left Back to projects link and item-section add CTA.
- 2026-06-26: Simplified the embedded Add client document module on `/builder/projects`: removed visible client checkbox, kept hidden client-visible default, and replaced path/reference with a full-width file picker under Title + Type fields.
- 2026-06-26: Merged the client document add form into the same compact project details module on `/builder/projects`, using the previously empty right-hand space inside the card.
- 2026-06-26: Tightened `/builder/projects` density so more projects/items fit on one page.
- 2026-06-26: In `feature/manual-entry-autofill-flow`, removed visible AI/spec-sheet checking, extraction processing, builder/admin review lane, workflow review queue, and AI approval UI from `/builder/projects`; replaced it with a single `Spec sheet automation — Coming soon` subsection while keeping manual item entry, database autofill, client access, documents, maintenance, and send-package flows. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Reduced `/builder/projects` cognitive load by moving client access controls and Add handover item into modal workflows.
- 2026-06-26: Expanded `/builder/projects` workspace width to use the available page area instead of leaving wide dead gutters, and changed the editable project overview area from side-by-side columns into stacked vertical cards. Project details now use a full-width vertical form; optional spec/import, client access, document entry, and product lookup sit underneath as separate vertical boxes, while the read-only status/sidebar remains on the right. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Filled out the standalone project workspace with a right-side non-editable information column. The sidebar now summarizes to-be-completed counts, autofill/review checks, missing manuals/warranties/compliance docs, common handover/legal document upload/confirmation prompts, item category counts with ready totals, and a demo-path reminder. The main editable item/detail flow remains on the left. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Reworked `/builder/projects` so opening a project no longer launches the old modal-style split workspace. The page now starts as a project browser; selecting a project opens a dedicated standalone project workspace on the same route with a polished project header, status summary, back-to-projects action, send-package action, and top navigation anchors for Overview, Items, Spec upload / review, Documents, and Paper trail. Existing manual item/autofill, optional spec/import, documents, client access, and send-package functionality remains wired. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-26: Created branch `feature/manual-entry-autofill-flow` for the temporary sales/demo manual-entry path. This branch does not abandon AI/spec extraction long term; extraction code remains in place as an optional secondary/import path. Implemented the first manual checklist/autofill vertical slice: `/builder/projects` now foregrounds Add handover item, approved product-database suggestions can autofill known identity/care/manual/warranty/source notes while remaining editable/reviewable, checklist filters cover search/status/category/missing section/source/review state, item cards expose a detail/edit panel for identity, location, supplier, quantity/finish/colour, care, manual, warranty, invoice, Code of Compliance, supporting docs/photos notes, builder notes, section statuses, and accepted-incomplete audit notes. Handover package preview and client portal package queries now prefer reviewed/accepted checklist items before optional workflow/extraction items. Verification: `npm.cmd run lint` passed and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings. Caveat: branch was created from an already dirty worktree containing prior extraction/Docling changes, so stage/commit carefully.
- 2026-06-24: Investigated user's correction that the spec has 80+ real items and prior Docling tests produced ~80-90 rows. Confirmed with `npm.cmd run spec-extract:smoke` that current Docling artifact still yields 81 deterministic proposals (`tableRowCount` 166, `signalRowCount` 130, `extractableRowCount` 80). Root cause for recent ~30 candidate runs was the all-row OpenAI text normalizer destroying markdown table structure before extraction: accepted normalized rows were written back as `| normalized_text | normalized_text |`, causing table labels/cells to dedupe/collapse and deterministic proposals to drop to 31. Patched `spec-text-normalizer.ts` to track original cells, require/ask for ` | ` cell-preserving normalized output, and only replace a table row when the normalized cell count/shape is safe; otherwise keep the raw table row and rely on final cleanup for display. Verification: `spec-extract:llm-smoke` with normalizer/classifier limit 120 now reports deterministicProposalCount/candidateCount/llmSentCount 81, llmAcceptedCount 89, llmEnhancedProposalCount 75; fixtures/lint/build passed.


This is the live worksheet for agents working on this repository from local Hermes, Codex CLI, Codex cloud/mobile, or any other coding agent. Keep it short, current, and honest.

## Mandatory Agent Startup

Before changing code or docs, read these files in this order:

1. `AGENTS.md`
2. `HANDOFF.md`
3. `WORKSHEET.md`
4. `docs/product-brief.md`
5. `docs/phased-work.md`
6. `docs/architecture.md`
7. Any phase-specific docs relevant to the task

## Git Push / Handoff Rule

Before any `git push`, prepare a detailed explanation covering:

- What was changed
- Why it was changed
- Files touched
- Checks/tests run and their results
- Remaining risks, unknowns, or follow-up work

After the push, update this worksheet and `docs/agent-handoff-log.md` with what is done and what should happen next.

## Current Ground Truth

- Local project path: `C:\Users\hunte\OneDrive\Desktop\TestWebApp`
- GitHub remote: `https://github.com/GeorgePersson/builder-handover-portal.git`
- Active branch at setup time: `codex/llamacloud-greenfield`
- Active manual/demo branch: `feature/manual-entry-autofill-flow`.
- Product direction: project checklist first. Builders manage handover items inside each project, with editable identity fields, care/manual/warranty/invoice/Code-of-Compliance/supporting-document sections, explicit completion statuses, database matching before search, no guessing on vague items, and paper-trail records for accepted-incomplete items. Specs/supporting documents still populate checklist candidates, but on `feature/manual-entry-autofill-flow` manual entry + approved database autofill is the primary happy path for sales/demo reliability.
- Stable planning docs: `HANDOFF.md`, `docs/product-brief.md`, `docs/phased-work.md`, `docs/architecture.md`, and `docs/project-handover-item-documentation-flow.md`.

## Done

- Next.js/TypeScript/Tailwind app scaffold exists.
- Builder, admin, and client portal routes exist.
- Specification upload, extraction preview/review, package preview, and publish scaffolds exist.
- Supabase-ready schema and local scaffold persistence exist.
- LlamaCloud/document-context adapter work exists on the active branch.
- Cloudflare pipeline/D1/R2 dry-run scaffolding and runbooks exist.
- Agent startup and handoff rules are now documented in `AGENTS.md` and this worksheet.
- Supabase readiness smoke script is available as `npm.cmd run supabase:smoke:readiness` and passed locally against the configured Supabase project.
- Source-gap approval hardening is ported from Codex cloud work: source-gap items cannot be approved as correct until missing fields/quote references/builder-info prompts are resolved, and builder-supplied approval requires a project-specific note.
- Extraction admin-noise guardrails now filter pure contract/payment/preliminaries/site setup/scaffolding/temporary works/council/insurance/health-and-safety/generic workmanship text before it becomes homeowner handover candidates while preserving warranties/manuals/certificates/products/finishes/maintenance.
- LLM-first source-backed spec review policy is implemented locally: unmatched source-backed candidates are LLM-eligible by default after junk/low-info/source-document skips, spend priority orders high-value rows before batching, LLM review lanes/reasons are preserved in review text, and API diagnostics expose reason/lane/error counts.

- Extraction admin-noise guardrails and quote/source-gap hardening from Codex cloud/mobile have been consolidated locally and pass smoke/lint/build checks.

- LlamaCloud/document-context readiness check exists as `npm.cmd run document-context:readiness`; current local check reports fallback to `local_pdf` until `LLAMA_CLOUD_API_KEY` is configured.

- Cloudflare-first deployment plan exists at `docs/cloudflare-nextjs-deployment-plan.md`; app host target is Cloudflare Workers/Pages via OpenNext, with Vercel fallback only for a proven blocker.

- New Docling local-first parser spike branch is `codex/docling-local-context`; plan docs are `docs/docling-local-context-plan.md` and `docs/docling-phased-work.md`. LlamaCloud stays in the architecture for later comparison.

- Docling local parser spike is implemented enough for local testing: `npm.cmd run docling:smoke:local` parsed the 34-page scanned spec into 89,871 markdown characters and 16 table-like structures.

- `DOCUMENT_CONTEXT_PROVIDER=docling_local` readiness now works and the app document-context contract can call the local Docling script with local-PDF fallback on failure.
- Manual-entry/autofill demo branch first slice is implemented on `feature/manual-entry-autofill-flow`: `/builder/projects` foregrounds manual Add handover item, approved product database suggestions can autofill known fields while remaining editable/reviewable, filters support larger checklists, item detail editing covers required handover sections and accepted-incomplete audit notes, and package/client portal read paths include reviewed checklist items.

## Needs Doing / Next Work

- Run a browser smoke for the manual-entry/autofill branch: open `/builder/projects`, add one purely manual item, add one database-autofilled item, edit all section fields/statuses, mark one complete or accepted incomplete, preview `/builder/handover-package`, publish if appropriate, then confirm `/client/portal` shows only homeowner-safe fields. The dev server was started briefly during implementation but did not emit logs before being stopped; no browser smoke was completed in this pass.
- Be careful before staging/committing this branch: it was created from a dirty worktree with many existing Docling/extraction changes. Stage only intended manual-flow files and any already-accepted prerequisite checklist files.
- UI rework notes are captured at `docs/project-workspace-ui-rework-notes.md`; do not implement them yet. Stay focused on proving the real upload -> Docling extraction -> review rows -> checklist candidates workflow first.
- Continue the checklist-first pivot after extraction-to-checklist sync: add database autocomplete/match review, add richer item detail editing/upload controls, connect guarded source search, and update publish-readiness/client-output mapping. Use `docs/project-handover-item-documentation-flow.md` as product truth.
- Extraction rows now sync into checklist candidates after document processing in both Supabase and local scaffold flows. Each synced checklist item carries source extracted item/document/job IDs, source evidence notes, extracted identity fields, autofilled-needs-review section statuses, and extraction sync metrics. Supabase has source columns plus unique source-extracted and legacy-extracted indexes verified through direct DB and REST. The current legacy specification upload has been backfilled to 85 checklist candidates from 93 review rows.
- Supabase checklist migration has been applied and verified: `project_handover_checklist_items` and `project_handover_checklist_events` exist, each is visible through REST (`200 []`), and four RLS policies are installed. The SQL file is now idempotent/public-schema-qualified for reruns.
- Checklist-first pivot first slice is implemented: shared checklist status/types/helper contract, local JSON persistence, Supabase migration, Supabase/local query/action paths, `/builder/projects` checklist dashboard, Add checklist item form, explicit not-enough-information prompt, and accepted-incomplete paper-trail action. `npm.cmd run lint` and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- Run the browser upload smoke with `npm.cmd run dev:docling` from Windows PowerShell, or `$env:DOCUMENT_CONTEXT_PROVIDER="docling_local"; npm.cmd run dev`; confirm extraction diagnostics show `provider: docling_local`, `fallbackUsed: false`, and useful checklist/review rows.
- Inspect/tune extraction prompts/guardrails only after the Docling-backed browser smoke shows real workflow output.
- Keep `.local-artifacts/docling/` ignored; do not commit parsed customer PDF outputs.
- Keep LlamaCloud code/docs available for a later quality comparison; do not remove the existing LlamaCloud architecture while adding Docling.
- Run the Supabase-mode browser workflow after Docling is wired: login, builder workspace/project, upload, extraction/review queue, source-gap handling, builder-supplied note, supporting evidence/quote path, publish readiness, publish, and client portal visibility.
- Keep publish readiness strict: unresolved source gaps, quote references, or builder-context prompts should not silently become client-facing. Approved-as-correct rows with lingering source-gap signals are counted as publish blockers unless edited, excluded, or marked builder-supplied with a note.
- Verify current app checks before pushing meaningful app changes: `npm.cmd run document-context:readiness`, `npm.cmd run supabase:smoke:readiness`, `npm.cmd run lint`, and `npm.cmd run build`; once implemented also run `npm.cmd run docling:smoke:local`.

## Last Updated

- 2026-06-24: User correctly challenged the architecture: cleanup cannot depend on a growing dictionary/audit list when the requirement is to send everything to the LLM. Root cause in `spec-final-cleanup.ts`: first final cleanup could run all rows, but the stricter repair retry was selected only from deterministic `qualityAudit` failures; if the audit did not know a new OCR class, no second strict LLM proofread happened. Changed final visible evidence boundary so, when OpenAI final cleanup is enabled, every final row gets a second strict LLM proofread pass (`buildStrictProofreadPrompt`) independent of dirty-word detectors/audit results. Deterministic audit now runs after LLM proofread as diagnostics/fail-closed review note only, not as the selector for what the LLM sees. Added response diagnostics `llm_proofread_sent_count` / `llm_proofread_accepted_count`; default final cleanup limit raised from 120 to 250; proofread can be disabled only with `OPENAI_SPEC_FINAL_PROOFREAD_ENABLED=false`. Verification passed: `spec-final-quality:fixtures`, `lint`, `build` (known Docling/Turbopack NFT warnings remain). Restart dev server before next upload so `llm_proofread_*` diagnostics appear.

- 2026-06-24: Investigated repeated visible OCR failure reported by user: `Waterproofmembraneunder tiles laidontimber floor inginwetareas ... veneerlaminated to astructuralplybase, straightlaid`. Current queue had 2 uploads / 76 rows; exact bad row was `e7dbbc46-5b70-4381-aff2-0da93db2c590` (`Engineered timber veneer flooring`). Root cause: all-row/final cleanup existed, but the generic glued-OCR segmenter/audit dictionary did not include flooring/waterproofing domain vocabulary (`membrane`, `tiles`, `timber`, `wet`, `areas`, `veneer`, `laminated`, `structural`, `ply`, `base`, `straight`, `laid`), so those long tokens were not recognized as dirty; this is why the same class of issue kept slipping despite AI cleanup. Added deterministic and audit fixes for `Waterproofmembraneunder`, `laidontimber`, `inginwetareas`, `veneerlaminated`, `astructuralplybase`, `straightlaid`, plus remaining current-row tokens `Timekeeperplustimer` and `Concealedwiring`. Extended generic segment/domain word lists for flooring/waterproofing vocabulary and strengthened final cleanup prompt examples. Added final-quality fixture `flooring_wet_area_glued_ocr_is_readable`. Backfilled the bad flooring row and towel-rail row `13622991-f6c0-4112-95e7-35899c7fb54c`; post-backfill audit across 76 rows for known dirty OCR/diagnostic/false-code patterns returned `flaggedCount=0`. Verification passed: `spec-final-quality:fixtures` (11), `spec-quality-audit:fixtures` (6), `spec-normalizer:fixtures` (4), `spec-extract:fixtures` (32), `lint`, and `build` (known Docling/Turbopack NFT warnings remain).

- 2026-06-24: Investigated user report `Whitefaceplates, ProductCode:` and audited current queue for similar visible field issues. Current queue had 54 review/checklist rows; systemic audit found 4 rows with visible OCR/field issues: PDL `Electrical Switching` (`Jconic`, `Whitefaceplates`, `PDLIconic`, `lifeeasy`, leaked `Has Identifier`/`Suggested Search Query`, and false `ProductCode: 2017` from `Winner of the 2017 Gold Design Award`), `Vantiy Waste`, `C/WSoft`, and `clickclackmushroom`. Root causes: deterministic product-code regex treated marketing years as codes; internal helper fields were being written into visible evidence; LLM classifier default cap was still 30 so not all candidates were guaranteed to reach AI; quality audit/normalizer missed the faceplate/code false-positive class. Changes: classifier is now default-on with `OPENAI_API_KEY` unless `OPENAI_SPEC_CLASSIFIER_ENABLED=false`, default classifier limit raised to 200; deterministic product-code extraction filters award/marketing years; `HasIdentifier`/`SuggestedSearchQuery` are no longer emitted and final cleanup strips legacy variants; added OCR cleanup/audit patterns for `Jconic`, `Whitefaceplates`, `PDLIconic`, `lifeeasy`, `Vantiy`, `C/WSoft`, and `clickclackmushroom`; strengthened final cleanup prompt examples. Added fixtures: spec-extract row `pdl icon series faceplates does not use award year as product code` and final-quality `pdl_faceplates_visible_text_is_readable_and_no_diagnostics`. Verification passed: `spec-extract:fixtures` (32), `spec-final-quality:fixtures` (10), `spec-quality-audit:fixtures` (6), `spec-candidates:fixtures` (5), `spec-llm:fixtures` (8), `lint`, `build` (known Docling/Turbopack NFT warnings remain). Backfilled current rows `7c18850c-356d-4ddb-a5ef-53b673af26f9`, `8486b7a9-58df-4be8-9e6f-c5289a4637cf`, `d1d9682c-084a-42ba-9eec-e19c46237058`, `dfdf99b6-7bea-424a-bc0c-a8d5e29388a6`; checklist `Electrical Switching` product_code is now null, visible evidence says `PDL Iconic Series White faceplates ... life easy`. Post-backfill audit found `flaggedCount=0` for the checked OCR/diagnostic/false-code patterns.

- 2026-06-24: Responded to user-caught final OCR spelling failure in current queue: `3. 1 flush jamb for architraves Cus to msize 2400 mm x 1500 mm Need to check size`. Root cause: final cleanup was defaulting to all-row AI when enabled, but pre-extraction normalizer was still env-gated/off by default and the quality audit did not know this split custom-size failure class, so the row could pass despite visible OCR garbage. Changed `spec-text-normalizer.ts` so OpenAI normalization is enabled by default when `OPENAI_API_KEY` exists unless `OPENAI_SPEC_NORMALIZER_ENABLED=false`, and added `OPENAI_SPEC_NORMALIZER_MODE=all|messy|off` defaulting to `all` with limit 200 so the source table rows are actually sent through AI cleanup instead of only hand-picked messy rows. Strengthened normalizer/final-cleanup prompts with `Cus to msize -> Custom size`, added deterministic cleanup/audit coverage for `Cus to msize` / `Custom msize`, and added final-quality fixture `flush_jamb_custom_size_spacing_is_readable`. Verification passed: `spec-final-quality:fixtures` (9), `spec-quality-audit:fixtures` (6), `spec-normalizer:fixtures` (4), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Backfilled current review row `1ee9eb73-665a-4013-8213-5fa14265e643` and checklist row `77210baa-6d40-4ae0-8011-e881225ba806` to show `flush jamb for architraves Custom size 2400 mm x 1500 mm Need to check size`.

- 2026-06-24: Fixed the multi-item-in-one-OCR-row miss the user caught on `Heated Towel Rail ... 1 x Insinkera tor multitap ...`. Root cause: the LLM prompt said classify candidates, but the application layer keyed classifications by `candidate_id` in a `Map`, so only one accepted classification per deterministic candidate could survive; the schema could not practically split a merged source row into multiple saved items. Changed `spec-llm.ts` to explicitly prompt the LLM to emit multiple classification objects with the same `candidate_id` when one source row contains multiple real items, grouped accepted classifications by candidate ID instead of overwriting them, and split one proposal into multiple output proposals. For split rows, evidence now prefers each classification's `source_quote` so the towel rail row does not pollute the Insinkerator row and vice versa; structured fallback fields from the parent proposal are suppressed for split child rows. Added cleanup for `Insinkera tor` -> `Insinkerator`. Added fixture `merged towel rail and insinkerator source row splits into two items` and updated the LLM fixture runner to support `classifications[]` / `expectedItems[]`. Verification passed: `spec-llm:fixtures` (8), `spec-candidates:fixtures` (5), `spec-final-quality:fixtures` (8), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Backfilled the current upload `7b5bff9b-e1b4-4174-a12b-9edfe34095b4`: inserted review row `605b8826-15a5-411d-858d-4b1e640e64db` / checklist row `aa8cbcef-214c-4418-ae9a-0de7300ba66a` for `Insinkerator Multitap` with model/code-needed review context.

- 2026-06-24: Investigated user report that the browser flow appeared to send nothing. Current DB state showed 1 latest upload (`87b90fca-c919-44c0-8d9e-c3df032e5b0e`, `Vsuvio_Contractor_Agreement_George_Persson.docx.pdf`) with 0 review rows and 0 checklist rows. Dev logs proved the pipeline did run: Docling parsed the PDF, deterministic extraction produced 1 candidate, LLM sent/accepted 1 classification, then proposedCount became 0 because the LLM adjudicator rejected it as a non-handover/admin/supporting-document item. Downloaded the uploaded PDF and confirmed it is an independent contractor agreement, not a building specification; deterministic source was a false positive from `manual testing` -> `Product warranties and maintenance manuals`, correctly dropped by LLM. Added zero-item handling: `process-pdf` now returns early with `saved_count: 0` before creating storage/upload/review rows when all candidates are rejected, response notes explicitly say no valid handover items were found after LLM validity review, and the upload panel now shows an amber no-items message without an "Open review queue" success link. Verified with a throwaway authenticated route call using the contractor-agreement PDF: HTTP 200, `saved_count:0`, no `storage`, no `specification_id`, classifier `1/1/0`, DB counts unchanged except the existing user upload remained. `lint` and `build` passed (known Docling/Turbopack NFT warnings remain). Existing empty user upload was left in place pending user decision.

- 2026-06-24: Finished LLM valid-item implementation with live-route enforcement. Extended `scripts/smoke-spec-route-quality.mjs` so route smokes now fail on broad invalid source-document/admin bundle rows such as producer-statements/code-compliance packages, in addition to visible OCR garbage. Static verification passed again: `spec-candidates:fixtures` (5), `spec-llm:fixtures` (7), `spec-final-quality:fixtures` (8), `spec-quality-audit:fixtures` (6), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Restarted dev server with Docling/OpenAI/final-cleanup all-mode and ran `npm.cmd run spec-route:quality-smoke`: upload `29ba0e0f-be5a-4a78-8bc8-aa6c2d886d45`, 53 saved review rows, 53 checklist rows, normalizer `50/46/4`, classifier sent/accepted/rejected `67/66/1`, final cleanup dirty-before/sent/accepted/rejected/dirty-after `2/53/53/0/0`, repair retry `0/0`, final quality audit `53/53` pass, bad visible review/checklist `0/0`, invalid producer/code-compliance review/checklist `0/0`. Direct DB inspection found zero source-doc-like saved rows and zero bad/invalid hits. Cleared smoke data afterward: deleted 53 checklist rows/events, 1 upload cascading 53 review rows, and 1 smoke org/user; final uploads/review/checklist/events/smoke-orgs `0/0/0/0/0`. Stopped the dev server started for the smoke.

- 2026-06-24: Added LLM valid-item adjudication so source-document/admin bundles do not become checklist items. Saved plan `.hermes/plans/2026-06-24_004650-llm-valid-item-adjudication.md`. Deterministic document/request-document rows now go through LLM classification instead of skipping the classifier. Updated classifier policy so broad bundles like `Producer statements and code compliance documents`, CCC/PS closeout packages, and generic certificate lists are `keep=false`/`note`, while specific products/systems/finishes needing manuals/warranties/certificates can still be kept as `needs_source_document`. Added fixtures proving the producer-statements bundle is dropped and a specific Grohe mixer warranty-source row is kept/formatted. Verification passed: `spec-candidates:fixtures` (5), `spec-llm:fixtures` (7), `spec-extract:fixtures` (31), `spec-final-quality:fixtures` (8), `spec-quality-audit:fixtures` (6), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Cleared stale run data afterward: deleted 72 checklist rows/events and 1 upload cascading 74 review rows; final uploads/review/checklist/events `0/0/0/0`.

- 2026-06-24: Pivoted OCR hardening away from exact-token whack-a-mole to a generic fail-closed readability repair gate. Saved implementation plan `.hermes/plans/2026-06-24_003900-fail-closed-readability-repair-gate.md`. Added generic glued-token segmentation/readability detection for unseen lowercase OCR compounds like `mountedonleftsidewall`, `slidesfromrighttoleftwhenfacingdoor`, and `shelftobemountedonrearwall`; added a final LLM repair retry for rows that fail the post-cleanup quality audit; unresolved rows now keep explicit `Evidence text needs manual cleanup (...)` review diagnostics instead of silently appearing clean. Response diagnostics now include `repair_retry_sent_count` and `repair_retry_accepted_count`. Fixtures now cover generic repair and fail-closed manual warning behavior. Verification passed: `spec-final-quality:fixtures` (8), `spec-quality-audit:fixtures` (6), `spec-normalizer:fixtures` (4), `spec-extract:fixtures` (31), `spec-candidates:fixtures` (4), `spec-llm:fixtures` (5), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Verified Supabase extraction queues are still clear: uploads/review/checklist/events `0/0/0/0`.

- 2026-06-24: User surfaced a fresh shower row with glued visible OCR (`overlapsbothfixedpanels`, `stand ingfacing`, `outsideof`, `shelfon`, `sideonend`, `bemountedonback`), proving the prior bad-token smoke list was too narrow even though the latest smoke passed. Added generic shower-direction/readability fixtures and deterministic cleanup/audit coverage for glued prepositions/directions/panel terms (`RHWhen`, `Whenst and ing`, `lookingat`, `outsideof`, `shelfon`, `sideonend`, `fixedpanel(s)`, `overlapsbothfixedpanels`, `mm-slides`, `NB:Slide`, `bemountedonback`). Verification passed: `spec-final-quality:fixtures` (6), `spec-quality-audit:fixtures` (4), `spec-normalizer:fixtures` (4), `spec-extract:fixtures` (31), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Cleared the stale failed rerun afterward: deleted 70 checklist rows/events and 1 upload cascading 72 review rows; final counts are uploads/review/checklist/events `0/0/0/0`.

- 2026-06-24: Cleared Supabase extraction state for a fresh user-flow rerun after the quality smoke. Before clearing, latest upload `3daef1f7-cc6c-451c-8133-8a02de0f7d28` had 57 review rows with bad visible count `0` and manual-quality-note count `0`. Deleted 170 legacy checklist candidates/events, 3 specification uploads (cascading 176 review rows), and 3 throwaway route-quality smoke org/auth users. Verified final counts: `specification_uploads=0`, `extracted_handover_items=0`, `project_handover_checklist_items=0`, `project_handover_checklist_events=0`, route-quality smoke orgs `0`.

- 2026-06-24: Implemented extraction quality hardening as a save-boundary invariant instead of one-off phrase patches. Classifier diagnostics (`LLMReviewLane` / `LLMReviewReason`) are no longer embedded in visible `extracted_text`; lane/reason are kept in structured proposal fields and review diagnostics. Final evidence cleanup now supports `OPENAI_SPEC_FINAL_CLEANUP_MODE=all|dirty|off` and in local/demo all-mode sends all final visible rows through cleanup in batches, then runs a separate final quality auditor for diagnostic leaks, split words, glued service/unit text, OCR misspellings, and suspicious token density. Added generic OCR/readability fixtures and route quality smoke scripts. Verification passed: `spec-extract:fixtures` (31), `spec-normalizer:fixtures` (4), `spec-candidates:fixtures` (4), `spec-llm:fixtures` (5), `spec-final-quality:fixtures` (5), `spec-quality-audit:fixtures` (3), `lint`, and `build` (known Docling/Turbopack NFT warnings remain). Fresh live Docling/OpenAI route quality smoke saved upload `3daef1f7-cc6c-451c-8133-8a02de0f7d28` with 57 review rows, 55 checklist rows, normalizer selected/accepted/rejected `53/51/2`, classifier sent/accepted/rejected `63/63/0`, final cleanup dirty-before/sent/accepted/rejected/dirty-after `2/57/56/1/0`, final quality audit pass `57/57`, bad visible review/checklist counts `0/0`.

- 2026-06-24: Audited the latest clean OCR spec rows for quality beyond dirty text and added systemic duplicate/category/readability improvements. Added fixture-backed guards for generic `Bathroom&`, `Fittings`, `Ceramic tiles Area`, `Raft Slab`, cladding maintenance inference, compliance-document OCR, and custom carpet OCR patterns; `spec-extract:fixtures` now has 31 fixtures. Added conservative final save-boundary dedupe/category normalization in `spec-final-cleanup.ts`. In-process Docling markdown + OpenAI smoke reports deterministic 65, final 57, final cleanup dirty-before/sent/accepted/rejected/dirty-after `5/5/5/0/0`, no suspicious generic titles, and only one intentional shared-source group (`Engineered timber veneer flooring` plus `Waterproof membrane under wet-area tiles`). Fresh live route smoke after dev-server restart saved upload `b747b824-08b3-462e-af4a-c872471d9702` with 60 review rows, 58 checklist rows, dirty review/checklist counts `0/0`, suspicious generic title count `0`, and the same intentional flooring/waterproofing shared-source group. Checks passed: `spec-extract:fixtures`, `spec-normalizer:fixtures`, `spec-candidates:fixtures`, `spec-llm:fixtures`, `lint`, and `build` (known Docling/Turbopack NFT warnings remain).

- 2026-06-24: Exposed final evidence cleanup diagnostics in the spec process API response as `final_evidence_cleanup` and tightened the final cleanup application path so, when the AI repairs `Description:` but leaves `source_snippet` dirty, the save-boundary gate can use the cleaned Description as readable source evidence instead of persisting glued OCR. Fresh Docling/OpenAI Supabase smoke on the real spec saved upload `fe738086-995f-4fab-a8bf-744d1444ecff` with `final_evidence_cleanup.dirty_after_count=0`; direct DB verification found `dirtyReviewCount=0`, no old dirty phrase hits, and `dirtyChecklistCount=0` across 63 synced checklist rows. Checks passed: `spec-extract:fixtures`, `spec-normalizer:fixtures`, `spec-candidates:fixtures`, `spec-llm:fixtures`, `lint`, and `build` (known Docling/Turbopack NFT warnings remain).

- 2026-06-23: Added a bounded LLM OCR/text normalization pass before extraction/classification.

- 2026-06-23: Added fixture-backed cleanup for remaining product category/title oddities.

- 2026-06-23: Added fixture-backed electrical category inference cleanup.

- 2026-06-23: Added a fixture-backed source-document de-duplication fix for Superior Kitchens quote rows. Pure source-document table rows such as `Kitchen`, `Scullery`, `Laundry`, `Entertainment Unit- drawers`, and the generic joinery product proxy are no longer emitted as separate product/review rows when the only evidence is `As per Superior Kitchens quote`; the single canonical `Kitchen and joinery supplier quote/source document` remains. Backfilled the current Supabase review queue by removing 6 redundant legacy source-document rows, leaving 87 review rows, 85 checklist candidates, and the two valid document rows. `spec-extract:fixtures`, `spec-extract:smoke`, `lint`, and `build` passed; build still shows known Docling/Turbopack NFT warnings.

- 2026-06-23: Inspected latest Supabase extraction/checklist state. Counts are `extracted_handover_items=93`, `specification_uploads=1`, `extracted_items=0`, `project_handover_checklist_items=0` before fix, proving the current browser upload path still uses legacy `extracted_handover_items` and was bypassing the newer `extracted_items` checklist sync. Added legacy checklist sync to `/api/specifications/process-pdf`, added/applied `legacy_extracted_handover_item_id` with a unique index, and backfilled the current 93-row upload into 85 non-document checklist candidates plus 85 sync events. Verification passed: `spec-extract:fixtures`, `spec-extract:smoke`, `lint`, and `build` (known Docling/Turbopack NFT warnings remain).

- 2026-06-22: Added fixture-backed OCR spacing cleanup for current extraction output: `Kitchen Isl and` -> `Kitchen Island`, `flushpanelpre-hung doors` -> `flush panel pre-hung doors`, `Asper Superior Kitchens quote` -> `As per Superior Kitchens quote`, plus related `underneathoverhead`, `privacysets`, `centreabove`, and `flushjamb` cleanups. Backfilled 18 current Supabase review rows. `spec-extract:fixtures`, `spec-extract:smoke`, `lint`, and `build` passed.
- 2026-06-22: Captured rough project workspace UI rework direction in `docs/project-workspace-ui-rework-notes.md`. Direction: clickable project browser, new-project confirmation/workflow modal, project workspace tabs (Overview, Items, Spec upload/item review, Documents, Paper trail, future Spec sheet builder), clean grouped item cards/detail views, status milestone module, builder-friendly review filters, hidden debug evidence during extraction tuning. Deferred implementation until extraction/checklist quality is proven.
- 2026-06-22: Wired extraction rows into checklist candidates. Supabase/local document processing now upserts checklist items from matched extracted rows, preserving source extracted item/document/job IDs, source evidence notes, extracted identity fields, and autofilled-needs-review section statuses. Applied and verified added Supabase source columns/index; REST select on source columns returned `200 []`. `npm.cmd run lint` and `npm.cmd run build` passed with known Docling/Turbopack NFT warnings.
- 2026-06-22: Applied the Supabase checklist migration using `SUPABASE_DB_URL`. Direct DB verification showed prerequisite tables present and created `project_handover_checklist_items` / `project_handover_checklist_events`; REST verification returned `200 []` for both tables. SQL was tightened to be public-schema-qualified and policy-idempotent.
- 2026-06-22: Implemented the first checklist-first pivot slice. Added `src/lib/project-handover-checklist.ts`, local scaffold persistence at `src/lib/server/local-store/project-handover-checklist.ts`, Supabase migration draft `docs/supabase-add-project-handover-checklist.sql`, query/action wiring, and a `/builder/projects` checklist dashboard with Add item and accepted-incomplete paper trail. `npm.cmd run lint` and `npm.cmd run build` passed; build still shows known Docling/Turbopack NFT warnings.
- 2026-06-22: Documented the investor-driven checklist-first pivot.
- 2026-06-22: Added a guarded "Try source search once" override for context-blocked product rows (e.g. generic-but-searchable items like hot water cylinders). The button appears on review cards/edit pages only for product rows in context/source/model-code request lanes, hides once `[source-search-attempted]` is written into `review_reason`, and server action enforces the same one-attempt limit for Supabase and local scaffold rows. If source enrichment finds a known source it moves the item back to admin review with the source note; if not, it leaves the item in `needs_model_code` with a manual-entry note rather than allowing repeated retries/spam. `lint` and `build` passed.
- 2026-06-22: Generalized the shower evidence fix for future uploads instead of one-row backfills. `spec-extract` now extracts descriptive model text when a known brand is followed by a non-numeric product configuration (e.g. Newline `Dry Fit 2 wall 900×900 Hinged RH`), parses `Type/Model/Size/Door/Niche` shower table headers into structured evidence fields, keeps full cleaned row text as the Description, and adds OCR phrase fixes for compacted terms such as `Waterproofmembraneunder`, `laidontimber`, `lookingat`, and `shelfon`. Added a regression fixture proving shower evidence preserves manufacturer, product code/model, size, door, niche, and hardware details. `spec-extract:fixtures`, `spec-extract:smoke`, `lint`, and `build` passed.
- 2026-06-22: Investigated `Showers Bed 1&2` evidence loss. Root cause: LLM application rebuilt `extracted_text` from only LLM fields and `classification.source_quote`, so when the model chose the short exact quote `Selected Newline shower`, it dropped richer deterministic/table evidence (`Type Model Size Door Niche`, `Dry Fit 2 wall`, `900×900`, `Hinged RH`). Patched LLM evidence assembly to preserve deterministic Manufacturer/ProductCode/Finish/Size fields when present and use the full deterministic `source_snippet` as Description. Backfilled current Newline shower rows so evidence now includes `Manufacturer/Supplier: Newline`, `ProductCode: shower Dry Fit...`, and the full source row. `lint` and `build` passed.
- 2026-06-22: Added temporary investor-demo existing-product matching. Seeded 10 approved Supabase `products`/`product_versions` with `TEMP_POC_DEMO` product_sources so they can be deleted later, added process-pdf matching that marks future extracted products as `matched_existing_record`/`auto_approved` when they match those DB records, and cleared the review queue for a fresh upload (`extracted_handover_items` 94→0, `specification_uploads` 1→0). Seeded demo products include Grohe/Elementi/Parisi/Heirloom/Art Ceram/Bagno items plus the heated towel rail. `lint` and `build` passed.
- 2026-06-22: Added investor-demo review UX polish: review metric cards are now clickable filters (`all`, `admin`, `context`, `ready`, `rejected`), the edit page shows a clear "what this needs" panel for `needs_model_code`, `needs_source_document`, `request_more_context`, and admin-review lanes, and the preview extraction list key now includes location/index to stop duplicate React key warnings for repeated LLM item titles. `lint` and `build` passed.
- 2026-06-22: Investigated repeated `llmSentCount: 10` after restart. `.env.local` had `OPENAI_SPEC_CLASSIFIER_LIMIT=120`, so the likely cause is a stale inherited shell/process env value of `OPENAI_SPEC_CLASSIFIER_LIMIT=10` overriding Next's dotenv load. Added `Spec LLM classifier config` diagnostics and a local `.env.local` guard so the effective max uses the larger of the process env and local env-file limit; cleared the latest 97-row/1-upload run from Supabase again. `lint` passed.
- 2026-06-22: OpenAI returned transient Cloudflare 520 during full PDF processing, causing deterministic fallback and saving 97 non-LLM rows. Added retry handling for retryable OpenAI statuses (408/409/429/5xx), lowered local classifier batch size to 10 with 3 retry attempts, preserved candidate diagnostics even when fallback occurs, verified a 10-candidate/5-batch LLM smoke succeeds, and cleared the failed 97-row upload from Supabase so the next browser rerun starts empty. `spec-llm:fixtures`, `lint`, and `build` passed.
- 2026-06-22: Implemented `.hermes/plans/2026-06-22_141615-llm-first-source-backed-spec-review.md`. Deterministic confidence is no longer a final skip gate for unmatched source-backed rows; `spec-candidates` now records eligibility reasons and spend priority, and `spec-llm` orders eligible rows by priority, batches OpenAI calls with `OPENAI_SPEC_CLASSIFIER_BATCH_SIZE`, keeps valid-but-incomplete rows in explicit lanes, and preserves LLM lane/reason while retaining broader deterministic snippets. Added no-network candidate and LLM-application fixtures plus richer API diagnostics (`eligible_by_reason`, `skipped_by_reason`, `sent_candidate_ids`, `accepted_by_lane`, `rejected_validation_errors`). Verification passed: `spec-extract:fixtures` (11), `spec-candidates:fixtures` (4), `spec-llm:fixtures` (4), `spec-extract:smoke` (93 proposals), `OPENAI_SPEC_CLASSIFIER_LIMIT=40 OPENAI_SPEC_CLASSIFIER_BATCH_SIZE=20 spec-extract:llm-smoke` (93 deterministic, 90 eligible, 40 sent across 20-size batches, 38 accepted, 2 rejected, 10,396 tokens), `lint`, and `build` (known Docling/Turbopack NFT warnings). Browser/Supabase rerun remains next because clearing/reprocessing live review rows was not performed in this code pass.
- 2026-06-22: Reviewed fresh full-flow output after raising the OpenAI classifier limit. The API saved 96 review rows, but only 8 were visibly LLM-touched; root cause for `Aluminium window and door joinery` was deterministic, not OpenAI: the window-joinery rule matched broad `/aluminium/`, so it chose the later electrical `Brushed aluminium coverplates`/pump row as evidence, and the LLM gate skipped Joinery because the category list did not include joinery/material-envelope categories. Fixed the rule to require window-joinery/translucent-laminate context, added fixtures proving aluminium cover plates alone do not create window joinery, expanded LLM gating to include joinery/cladding/waterproofing/structural/linings, and added API response/console `ai_classifier` diagnostics so future runs show candidate, needs-LLM, sent, accepted/rejected, and token counts. Fixtures/smoke/lint/build passed.
- 2026-06-22: Reworked the service/equipment extraction miss exposed by page 31 into a systemic table-row improvement rather than a one-off `Ducted air conditioning` patch. Blank/repeated Docling table rows with service assets now survive row filtering, infer canonical service titles from value cells, and classify heating/cooling rows as `needs_model_code` before finish/general rules. Added regression fixtures for generic blank repeated air-conditioning and underfloor-heating rows, added extraction audit output to `npm.cmd run spec-extract:smoke`, and narrowed the old broad `Heat pump system` fallback so generic `heating` text no longer creates a wrong item. Smoke now reports 93 proposals from the cached Docling artifact and includes `Ducted air conditioning`, `Hot water cylinder`, `External pump power connection`, and `Solar power prewire`; fixtures/lint/build passed.
- 2026-06-22: Added the OpenAI second-pass spec classifier behind the deterministic Docling pipeline. New `src/lib/ai/spec-candidates.ts` builds source-backed candidates and gates LLM spend; `src/lib/ai/spec-llm.ts` calls OpenAI Responses with strict JSON schema, validates source quotes/title support, normalizes 0-1 vs 0-100 confidence, and can apply accepted classifications back to proposals. API extraction routes now optionally run this when `OPENAI_SPEC_CLASSIFIER_ENABLED=true`; otherwise deterministic extraction remains the fallback. Added `npm.cmd run spec-extract:llm-smoke`; latest limited smoke sent 6 candidates, used 1,705 tokens, accepted 6, rejected 0, and kept final proposal count at 90. Fixtures/smoke/lint/build passed.
- 2026-06-22: Re-ran the full local Docling PDF conversion and reviewed extraction output against fixtures. Added 4 more systemic fixtures for repeated floor headings, generic paint duplicates, `Bathroom&` ceiling rows, and `Please Note:` electrical-position notes. Fixed evidence scoring to prefer pattern-matching row evidence over repeated headings, normalized `Door stops`/GIB ceiling OCR, and suppressed generic note rows. Fixture suite is now 7 cases; Docling PDF rerun completed 34 pages/89,871 chars/16 tables; smoke now returns 90 proposals with no repeated-heading/generic-title suspicious matches in the focused review. Fixtures/smoke/lint/build passed.
- 2026-06-22: Started the systemic Docling extraction rework instead of one-off row fixes. Added `npm.cmd run spec-extract:fixtures` with targeted row-regression fixtures, split normalization/evidence/classification helpers into `src/lib/ai/spec-normalize.ts`, `src/lib/ai/spec-evidence.ts`, and `src/lib/ai/spec-classify.ts`, and rewired `spec-extract.ts` to use those stages. Fixture/smoke/lint/build all pass; smoke remains 96 proposals. Future bad rows should be added to `scripts/fixtures/spec-extract-row-fixtures.json` before changing extraction logic.
- 2026-06-22: Adjusted finish/general handover items after competitor workflow feedback. Tiles, paint, carpet/flooring, and doors now get review notes that warranty docs may not be required; builders should confirm supplier/range/colour/location/project selection instead. Fixed rule evidence selection so `Interior flush panel doors` uses the actual door row instead of the repeated `INTERIOR DOORS` heading, and cleaned duplicated paint/tile evidence. Backfilled the current Supabase review rows for interior doors, paint, wall tiles, and garage carpet. Smoke/lint/build passed.
- 2026-06-22: Replaced the review queue "Request context" action with non-clickable context flags, because context classification should tell builders what is missing before source search rather than act like a manual button. Also cleaned Docling duplicate table cells and OCR spacing for Builder's-range tiling rows; the bathroom wall-tiling example now reads as `Wall Tiling- Bathroom(freestanding bath)` with a single clean source snippet and `request_more_context` action. Smoke/lint/build passed with known Docling/Turbopack warnings.
- 2026-06-22: Pushed first-pass request-more-context classification to `codex/docling-local-context` at `e52e623`. Extraction still returns 96 proposals, but actions now split into standard review plus `needs_model_code`, `needs_source_document`, and `request_more_context`; latest 100-row upload was backfilled with review notes (32 context-request rows visible via notes). Smoke/lint/build passed; direct enum migration via Supabase CLI was blocked by local 5432 network access, so runtime falls back to `admin_review` status plus explicit review notes until `docs/supabase-add-extracted-item-context-statuses.sql` is applied.
- 2026-06-22: Added first-pass OCR/readability normalization and compact title dedupe in `src/lib/ai/spec-extract.ts`. Smoke changed from 97 to 96 proposals because duplicate Garage/Grohe case variants collapsed while keeping broad recall; examples improved include `Blockwork on footing`, `Cavity Slider Handles`, `Parisi Toilet`, `Toilet roll holder`, `Heated Towel Rail`, and `Gas heating`. Smoke/lint/build passed with known Docling Turbopack warnings.
- 2026-06-21: Full fresh Docling workflow produced upload `96386625-c705-4895-9f89-40c09a899d04` with 100 review rows (95 product, 3 maintenance, 2 document). Assessment: keep refining Docling for now because it is local/PAYG-friendly and now finds useful breadth, but add an Azure Content Understanding comparison spike for quality benchmarking before committing to a provider switch.
- 2026-06-21: Pivoted Docling extraction toward the provided Azure Context Understanding schema: added table-row item extraction with structured evidence fields (Name, Manufacturer/Supplier, ProductCode, Finish, Size, Category, Location, Description, HasIdentifier, SuggestedSearchQuery). Smoke now finds 97 proposals; latest upload backfilled from 25 to 95 review rows.
- 2026-06-21: User provided prior Azure Context Understanding nested item schema; key lesson is extraction should be exhaustive item-centric structured rows (Name, Manufacturer/Supplier, Range/Model/Code, Finish/Colour/Size/Qty, Category/Location, Description/Notes, HasIdentifier/SearchQuery), not a small hard-coded rule list.
- 2026-06-21: Tightened Docling evidence snippets to prefer original table rows over merged chunks; Grohe mixer now backfills as `Grohe,Essence Kitchen Mixer with pullout spray-brushed warm sunset 30270 DLO Kitchen Mixer` instead of adjacent shower/footer noise.
- 2026-06-21: Fixed review-source issue from the 25-row Docling run: future extracted rows now persist `source_snippet`, Docling image/comment artifacts are stripped from evidence text, and the latest upload rows were backfilled with real snippets.
- 2026-06-21: Pushed broader Docling extraction implementation to `codex/docling-local-context` at `214872f`; next retry should reprocess the PDF and inspect the ~27-row review queue.
- 2026-06-21: Broadened `buildSpecificationProposals()` into a Docling-aware deterministic extractor; smoke test on the real Docling markdown now returns 27 proposals (21 products, 4 maintenance, 2 documents) instead of 5.
- 2026-06-21: Anchored Docling workflow result: parse/upload finished, only 5 review items were inserted, and the next bottleneck is the keyword-only `src/lib/ai/spec-extract.ts` proposal builder.
- 2026-06-21: Fixed spec process API storage upload to use the server-only Supabase service role when available and return/log storage detail for future failures.
- 2026-06-21: Added Next dev origin allowlist for `127.0.0.1`/`localhost` and native file-input resync on mount to recover after Fast Refresh/HMR.
- 2026-06-21: Fixed spec upload UX so Process to review no longer stays greyed out when the browser/file input state gets out of sync; click now validates missing PDF with an inline error.
- 2026-06-21: Added Windows-friendly `npm.cmd run dev:docling` script after PowerShell rejected POSIX-style inline env syntax.
- 2026-06-21: Pushed Docling local parser implementation to `codex/docling-local-context` at commit `1a79f03`; next task is browser upload smoke with `DOCUMENT_CONTEXT_PROVIDER=docling_local`.
- 2026-06-21: Implemented Docling local CLI smoke and `docling_local` provider wiring; real scanned spec parsed to 89,871 markdown chars / 16 tables, readiness/supabase/lint/build passed, with Turbopack NFT tracing warnings to revisit.
- 2026-06-21: Pushed Docling planning branch `codex/docling-local-context` at commit `8565528`; next task is local Docling install/CLI conversion against the real scanned spec.
- 2026-06-21: Started Docling local-first parser branch `codex/docling-local-context`; next work is a local Docling CLI spike, then `docling_local` provider wiring and real scanned-PDF smoke.
- 2026-06-21: Pushed phone/Codex cloud consolidation to `codex/llamacloud-greenfield` at commit `e3bfe66`; next step is LlamaCloud config plus full Supabase workflow smoke.
- 2026-06-21: Anchored after phone/Codex cloud consolidation: merged Cloudflare deployment plan, LlamaCloud readiness, extraction guardrails, and quote/source-gap hardening; local readiness, Supabase smoke, lint, and build passed.
- 2026-06-21: Hardened quote/source-gap approval and readiness checks so Supabase approvals load source-gap fields, approved-as-correct gaps remain publish blockers, and supporting-evidence uploads include audit metadata.
- 2026-06-21: Hardened extraction guardrails for admin/legal/contract/preliminaries/site setup noise; lint passed in Codex cloud and local verification is pending.
- 2026-06-21: Added secret-safe LlamaCloud/document-context readiness reporting and documented the LLAMA_CLOUD_API_KEY configuration path.
- 2026-06-21: Pushed consolidated Supabase smoke/source-gap readiness work to `codex/llamacloud-greenfield` at commit `9d8ff14`.
- 2026-06-21: Consolidated selected Codex cloud branches: Supabase readiness smoke plus source-gap/publish-readiness hardening; lint/build passed.
- 2026-06-21: Added bedtime Codex cloud/mobile handoff prompt and clarified that local Hermes cannot continue after the computer is off.
- 2026-06-21: Pushed Supabase migration verification and agent skills setup to `codex/llamacloud-greenfield` at commit `9332619`.
- 2026-06-21: Applied all repo Supabase add-migrations, verified missing REST tables now exist, and installed Supabase agent skills.
- 2026-06-21: Pushed cross-agent worksheet/setup documentation to `codex/llamacloud-greenfield` at commit `ead6510`.
- 2026-06-21: Added cross-agent ground-truth workflow and push/handoff discipline.
- 2026-06-21: Verified Supabase service-role access, created/confirmed `handover-documents`, and documented remaining migration gaps.

## Setup Status

### Supabase

- `.env.local` has Supabase URL, anon key, service role key, and direct Postgres `SUPABASE_DB_URL` configured locally. Do not commit `.env.local`.
- Service-role REST access and direct Postgres migration access were verified against the Supabase project.
- Private storage bucket `handover-documents` was created/confirmed for app uploads. An older underscore bucket `handover_documents` also exists but the app expects the hyphenated bucket.
- All repo Supabase add-migrations in `docs/supabase-add-*.sql` were applied successfully on 2026-06-21.
- REST verification now passes for `uploaded_documents`, `document_extraction_jobs`, `project_credit_events`, `handover_open_events`, `document_download_events`, and `handover_approvals`.
- Database verification confirms the newer workflow tables, project credit tables, handover event/approval tables, expected enums, and `ensure_builder_workspace` / `accept_project_client_invite` RPCs exist.

### LlamaCloud

- Not configured yet. This does not block local development because `DOCUMENT_CONTEXT_PROVIDER=local_pdf` can continue using the local PDF fallback.
- It does block validation of the preferred LlamaCloud Parse path for real PDFs, scanned/table-heavy specs, latency, and failure handling.

### Test Spec

- User provided a real outline spec PDF at `C:\Users\hunte\Downloads\2074 legal signed outline spec.pdf.pdf` for extraction/upload testing. Initial `pdf-parse` check found 34 pages but only ~957 text characters, so it is likely scanned/image-heavy; local plain-text parsing alone is not enough for realistic extraction. LlamaCloud or OCR-capable processing will be important for this file.

### Agent Skills

- Installed Supabase agent skills via `npx skills add supabase/agent-skills --yes`. The repo now has `.agents/skills/supabase`, `.agents/skills/supabase-postgres-best-practices`, and `skills-lock.json` so compatible coding agents can load Supabase-specific guidance.

## Bedtime / Phone Codex Cloud Prompt

Use this from Codex cloud/mobile when handing off from local Hermes:

```txt
Continue the Builder Handover Portal on branch codex/llamacloud-greenfield. First read AGENTS.md, HANDOFF.md, WORKSHEET.md, docs/product-brief.md, docs/phased-work.md, docs/architecture.md, and docs/agent-handoff-log.md.

Current state: Supabase URL/anon/service-role/direct Postgres access were configured locally by Hermes, the private handover-documents bucket exists, all docs/supabase-add-*.sql migrations were applied successfully, and Supabase REST verification passed for uploaded_documents, document_extraction_jobs, project_credit_events, handover_open_events, document_download_events, and handover_approvals. Supabase agent skills are committed under .agents/skills.

Do not assume secrets are available in Codex cloud unless configured there. If cloud secrets are missing, do code/docs work only and document exactly which secret is needed. Do not print secrets.

Next best task: run or prepare the Supabase-mode app smoke test for magic-link login, builder workspace bootstrap, project/spec upload, extraction/review queue creation, and publish-readiness behavior. The real test PDF is scanned/image-heavy, so plain local pdf-parse sees very little text; LlamaCloud/OCR is still needed for realistic extraction. If you cannot access the local PDF from cloud, add a clear test checklist or use repo demo assets instead.

Before any git push, write a detailed explanation of what changed, why, files touched, checks run, and remaining risks. After pushing, update WORKSHEET.md and docs/agent-handoff-log.md.
```

## 2026-06-21 - Cloudflare-first Next.js app deployment plan

- Completed a docs-only Cloudflare-first deployment plan for hosting the Next.js 16 product app on Cloudflare Workers with the OpenNext Cloudflare adapter.
- Added compatibility findings, package/config changes, environment variable handling, Supabase key guidance, routes needing workerd review, deployment commands, validation steps, and risks in `docs/cloudflare-nextjs-deployment-plan.md`.
- Next recommended task: implement the smallest OpenNext/Wrangler product-app config PR, then run `npm run preview` against Supabase + LlamaCloud configuration before any custom-domain cutover.
